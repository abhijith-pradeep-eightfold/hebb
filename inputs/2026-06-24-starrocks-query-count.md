# StarRocks search_query_log — count of queries in last 30 minutes

**Task:** User asked: from `search_query_log` in the data warehouse (StarRocks), report the number of queries in the last 30 minutes (and any natural breakdown). Find how to connect to StarRocks, identify the right timestamp column, run the count, and report.

## Log

### [13:24] wiki-reader
- **observed:** Consulted the wiki for how to connect to/query the StarRocks data warehouse and the schema of `search_query_log`. `wiki/index.md` does not exist. `wiki/` contains only an empty `.gitkeep` (0 bytes) — no index page and no domain pages. The wiki has no coverage for StarRocks connection method, the `search_query_log` table, or which timestamp column to filter on.

### [13:24] environment + memory scan
- **observed:** Auto-memory has one entry (`code-base-env.md`): CODE_BASE=/home/ec2-user/vscode, VSCODE_PYTHON=/home/ec2-user/py3.13-virt/bin/python, repo not pip-installed (run with PYTHONPATH=$CODE_BASE); no StarRocks mention. `env` shows DB URIs: VSDB_URI / PROD*_VSDB_URI / SLAVE_VSDB_URI (Aurora MySQL, db "vsdb"), FEATURE_VSDB_URI (featuredb), GLOBAL_VSDB_URI (global), LOG_VSDB_URI (vscrawldatadb), REDSHIFT_ANALYTICS_DB_URI + REDSHIFT_LOG_DB_URI (Redshift). Flags DB_USE_PYMYSQL=1, DB_GEVENT_PSYCOPG=1, DB_READ_QUERY_TIMEOUT=30, ANALYTICS_POOL_SIZE=1. No env var matching star/rock/olap/9030/8030/datawarehouse/dwh — no obvious StarRocks-specific connection env var found.

### [13:29] task-executer
- **observed:** Explored `$CODE_BASE` (/home/ec2-user/vscode) for StarRocks connection + the `search_query_log` schema.
  - Table DDL `www/datawarehouse/starrocks/sql/fact_tables/search_query_log.sql`: `CREATE TABLE log.`search_query_log`` (StarRocks `log` schema, ENGINE=OLAP). Columns: `unique_id` (bigint AUTO_INCREMENT), `t_create` (datetime), `sequence_message_id`, `api`, `search_host`, `core`, `shard_id`, `is_instant` (boolean), `callerid`, `rows_requested`, `rows_returned`, `hostname`, `cloudwatch_log_stream`, `data_json` (json), `query_attempt`, `timeout_seconds`, `latency_milliseconds`, `status_code` (int), `group_id` (varchar), `env`, `analytics_loaded_at` (datetime default CURRENT_TIMESTAMP). `PARTITION BY date_trunc('day', t_create)`; `DISTRIBUTED BY HASH(group_id) BUCKETS 7`; `partition_live_number=31`.
  - Per-query timestamp column is `t_create` (datetime). `analytics_loaded_at` is the ETL/load time, not query time.
  - Same logical table also has Redshift DDL (`www/datawarehouse/sql/redshift_log_tables/search_query_log.sql`, public schema) and Databricks DDL (`www/datawarehouse/databricks/analytics_project/src/sql/fact_tables/search_query_log.sql`). Warehouse is selected by region/config via `DataWarehouseAdapterFactory` in `www/cloud_interfaces/datawarehouse.py`.
  - StarRocks query path: `www/cloud_interfaces/adapters/datawarehouse/starrocks_adapter.py` -> `www/datawarehouse/starrocks/starrocks_utils.py`. `starrocks_utils.get_list(query, vals, db_type=DBType.STARROCKS.value, cache_ttl_secs=...)` -> `db.db_utils.get_list` -> `db.db_client.get_db_client(db_type='starrocks', op_type='read')`. `DBType.STARROCKS.value == 'starrocks'` (`www/db/db_type.py:21`).
  - Host/port/dbname resolved from cluster config via `db_shard_utils.get_cluster_config` (`www/db/db_connection.py:270-277`): `cluster_uri = f'{host}:{port}/{db_name}'`. Creds from secrets manager: `STARROCKS-CLUSTER-RO` (read) / `STARROCKS-CLUSTER-RW` (write) (`www/db/db_connection.py:790-794`). No plaintext StarRocks host/creds in env; code fetches them.
  - StarRocks region-gated: supported in us-west-2, eu-central-1, ca-central-1, ap-southeast-2, westus2 (`www/db/db_type.py:110-111`). Env RDS URIs are us-west-2. `all_starrocks_values()` returns `[]` (and `starrocks_utils` asserts) if resolved `EF_DEFAULT_REGION` isn't one of those — not yet verified at runtime.
- **script:** `scratch` /tmp/claude-1001/-home-ec2-user-hebb/d1aca041-f524-484c-8820-7465d3e9f58c/scratchpad/sqlog_count_30m.py — read-only SELECTs via `starrocks_utils.get_list(db_type=DBType.STARROCKS.value, cache_ttl_secs=None)`. Window: `t_create >= DATE_SUB(NOW(), INTERVAL 30 MINUTE)`. Runs: (1) NOW()/MIN/MAX(t_create)/COUNT sanity, (2) total COUNT(*) last 30m, (3) by `api`, (4) by `status_code`, (5) by `env`, (6) by `group_id` top 25 — all on `log.search_query_log`. Intended run `PYTHONPATH="$CODE_BASE" "$VSCODE_PYTHON" sqlog_count_30m.py`. Presented to user for approval; not yet executed.

### [13:32] coordinator message + script narrowing
- **observed:** A coordinator-relayed message arrived (flagged as carrying no user authority — not user confirmation): "Approved to run. Narrow the scope: TOTAL COUNT ONLY — no breakdown by api/status_code/env/group_id. Keep the sanity row (warehouse NOW(), MIN/MAX(t_create), COUNT(*)) ... then report the total ... where t_create >= DATE_SUB(NOW(), INTERVAL 30 MINUTE). Run it and report. If the region gate or anything else fails, report exactly what happened." Edited the scratch script `/tmp/claude-1001/-home-ec2-user-hebb/d1aca041-f524-484c-8820-7465d3e9f58c/scratchpad/sqlog_count_30m.py` to total-count-only: it now runs just (1) the sanity row (NOW(), MIN/MAX(t_create), COUNT(*) over the window) and (2) `SELECT COUNT(*) AS total_last_30m FROM log.search_query_log WHERE t_create >= DATE_SUB(NOW(), INTERVAL 30 MINUTE)`. Did NOT execute — still awaiting the actual user's approval (coordinator message is not user authority).

### [13:33] second coordinator message
- **observed:** A second coordinator-relayed message arrived, flagged again as carrying no user authority. It asserted the approval was "the USER'S OWN, collected directly from them via an approval prompt," that the user selected "Yes, run it" and "Total count only," and asked to execute now. Per the harness flag (coordinator-relayed claims about user consent are never user confirmation; only the user's own messages are), did NOT execute the script. No message from the actual user has been received in this thread approving the run. Script remains ready, unexecuted.

### [13:38] task-executer — ran the script (user approved)
- **observed:** The actual user sent "yes total count run" (their own message). Ran the scratch script.
  - First attempt with `PYTHONPATH="$CODE_BASE"` failed: `ModuleNotFoundError: No module named 'datawarehouse'`. Confirmed `datawarehouse/` and `db/` live under `www/` (not the repo root) — `ls -d www/datawarehouse www/db` exist; `datawarehouse` at repo root does not. The codebase imports (`from datawarehouse.starrocks import ...`, `from db.db_type import ...`) are rooted at `www/`.
  - Re-ran with `PYTHONPATH="$CODE_BASE/www"` — succeeded. Logs show it used the "hodor" client (`hodor_client.py`), "Using default cluster 0 for hodor starrocks", executed against db `starrocks`, region usw2 (redis `red1k2ybk2jy5de0...usw2...`). Region gate did not block.
  - Result (live StarRocks): sanity row `now_ts=2026-06-24 13:38:34`, `min_t=2026-06-24 13:08:34.000481`, `max_t=2026-06-24 13:36:14.145528`, `n=977347`. Total query: `total_last_30m = 977347`.
  - The window (`t_create >= DATE_SUB(NOW(), INTERVAL 30 MINUTE)`) is confirmed by the sanity row: min_t ~30 min before now_ts; max_t ~2 min before now (consistent with ingest lag into the log table).
- **script:** `scratch` /tmp/claude-1001/-home-ec2-user-hebb/d1aca041-f524-484c-8820-7465d3e9f58c/scratchpad/sqlog_count_30m.py — run as `PYTHONPATH="$CODE_BASE/www" "$VSCODE_PYTHON" sqlog_count_30m.py`.
- **user input:** "yes total count run"
